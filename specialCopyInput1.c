#include <stdio.h>
#include <stdlib.h>
//Copies I to O, but replaces each string of 1+ blanks with just one
/*
int main()
{
	int c;
	int bool;
	bool = 0;
	while((c=getchar())!=EOF){
		if(c==' '&&!bool){
			putchar(c);
			bool = 1;
		}else if(c!=' '){

			putchar(c);
		}
	}

	printf("%d\n%d\n",EOF);

	return 0;
}
*/


